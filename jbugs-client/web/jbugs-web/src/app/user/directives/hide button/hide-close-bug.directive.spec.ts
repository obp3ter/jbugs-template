import { HideCloseBugDirective } from './hide-close-bug.directive';

describe('HideCloseBugDirective', () => {
  it('should create an instance', () => {
    const directive = new HideCloseBugDirective();
    expect(directive).toBeTruthy();
  });
});
